name: Auto GitHub Contributions

permissions:
  contents: write
  actions: read
  checks: write
  
on:
  schedule:
    - cron: '*/30 * * * *'   # runs every hour
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.x"

      - name: Run updater script
        run: python .github/scripts/updater.py

      - name: Commit & Push changes
        run: |
          git config --global user.name "Om-mac"
          git config --global user.email "185115868+Om-mac@users.noreply.github.com"
          git add .
          git commit -m "Auto: $(date)" || exit 0
          git push https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/Om-mac/Plant_factory.git
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
